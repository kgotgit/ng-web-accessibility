import { ListboxDirective } from './listbox.directive';

describe('ListboxDirective', () => {
  it('should create an instance', () => {
    const directive = new ListboxDirective();
    expect(directive).toBeTruthy();
  });
});
